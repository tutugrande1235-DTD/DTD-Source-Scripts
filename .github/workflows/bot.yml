name: DOT Buddy

on:
  issue_comment:
    types: [created]
permissions:
  issues: write

jobs:
  reply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Executar botcode.js
        run: node .github/workflows/botcode.js
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          ISSUE_NUMBER: 7